var removeDuplicates = function(nums) {
    
    let i = 0; j = 0,k=0;
    while(j < nums.length)
    {
        if(nums[i] != nums[j])
         { nums[k] = nums[i];
         i = j;
         k++;
         }
        else{
            j++
        }  
    }
    nums[k] = nums[i];
    return k+1;
};