var singleNumber = function(nums) {
    nums.sort((a,b)=>a-b);
    for(let i = 0;i < nums.length; i+=2)
    {
        if(i+1 <= nums.length-1){
          if(nums[i] != nums[i+1])
          return nums[i];
        }
        else{
             return nums[i];

        }
    }
};